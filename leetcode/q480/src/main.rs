struct Solution;

impl Solution {
    pub fn median_sliding_window(nums: Vec<i32>, k: i32) -> Vec<f64> {
        let result = vec![0; nums.len() - k as usize];
        todo!()
    }
}

fn main() {
    println!("Hello, world!");
}
